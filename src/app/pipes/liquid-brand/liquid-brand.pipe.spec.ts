import { LiquidBrandPipe } from './liquid-brand.pipe';

describe('LiquidBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new LiquidBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
